import "./Cubic.css";

const Cubic = (props) => {
  //   const classes = "cubic " + props.bgColor;
  return (
    <div className="cubic" style={{ backgroundColor: props.bgColor }}></div>
  );
};

export default Cubic;
